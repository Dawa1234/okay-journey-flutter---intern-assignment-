import 'package:flutter/material.dart';

ThemeData baseDesign = ThemeData(
  appBarTheme: const AppBarTheme(
      centerTitle: true,
      titleTextStyle: TextStyle(fontSize: 20, color: Colors.white)),
  primarySwatch: Colors.orange,
);
